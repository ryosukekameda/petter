<div class="listings">
  <div class="listings_channels">
    <h2 class="listings_header">admin</h2>
    <ul class="channel_list">
      <% if admin_signed_in? %>
        <%= sidebar_link_item('トップ', admin_path) %>
        <%= sidebar_link_item('ユーザー一覧', admin_users_path) %>
        <%= sidebar_link_item('エディット', edit_admin_user_path(current_user)) %>
        <%= sidebar_link_item('ログアウト', destroy_admin_session_path) %>
      <% else %>
        <%= sidebar_link_item('トップ', admin_path) %>
        <%= sidebar_link_item('ログイン', new_admin_session_path) %>
      <% end %>
    </ul>
  </div>
</div>
